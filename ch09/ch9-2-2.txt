我正在使用n8n的Code節點（JavaScript模式）。
請幫我撰寫一段程式碼，功能如下：

1. 從$input.all()取得前一節點的資料，每筆資料格式為{ 姓名, 年齡, 部門 }
2. 依照「部門」分組，計算每個部門的平均年齡（四捨五入至整數）
3. 回傳格式需符合 n8n 的標準：return [{json: {...}}]
   其中 json 內容為 { 部門名稱: 平均年齡 } 的物件

請只回覆程式碼，不需要額外說明。

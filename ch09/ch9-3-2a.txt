請幫我產生一個完整的HTML網頁，用於n8n Respond to Webhook節點的回應內容。
前一節點輸出的JSON資料範例：
［貼上JSON範例，例如： 
{
  "order_id": "ORD001",
  "customer": "王小明",
  "amount": 1500,
  "status": "已確認"
}］
需求：
- 以清楚的版面顯示上述欄位內容，使用n8n的Expression語法嵌入值，例如 {{ $json["order_id"] }}
- 設計一個訂單確認結果頁面，包含標題、各欄位的標籤與值
- 頁面需具備基本RWD響應式設計，適合手機與電腦瀏覽
- 加入返回按鈕或關閉提示
- 使用內嵌CSS樣式，不引用外部套件
請回傳完整的HTML程式碼。

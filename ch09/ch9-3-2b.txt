請幫我產生一個完整的HTML網頁，用於n8n Respond to Webhook節點的回應內容。
前一節點輸出的JSON陣列資料範例：
［貼上JSON陣列範例，例如：
[
  { "product": "商品A", "qty": 2, "price": 500 },
  { "product": "商品B", "qty": 1, "price": 800 }
]］
需求：
- 使用n8n的Expression語法搭配 $json 取得資料，或使用Code節點將資料轉為HTML表格字串後以 {{ $json["html_table"] }} 嵌入
- 以HTML表格呈現所有資料列，包含表頭與每一筆明細
- 表格底部加入合計列，計算數量總和與金額總計
- 加入基本的表格樣式，例如交替行底色、置中對齊
- 使用內嵌CSS樣式，不引用外部套件
請回傳完整的HTML程式碼。
